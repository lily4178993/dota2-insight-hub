import React from 'react';

function ItemsPage() {
  /* cconst { items: allItems } = useSelector(selectAllItemsState);

  onst { items: beginnerItems, categories: beginnerCategories } =
  getBeginnerCategorizedItems(allItems);
  const { items: expertItems, categories: expertCategories } =
    getExpertCategorizedItems(allItems); */

  return (
    <>
      <h2>Beginner Items</h2>
      <div>nothing for now</div>
    </>
  );
}

export default ItemsPage;
